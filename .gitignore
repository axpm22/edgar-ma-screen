# 9.2 GB of cached SEC downloads and derived databases. Fully rebuildable
# with `make all`; must never enter git history.
data/
logs/
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.duckdb
*.parquet
*.pdf
docs/wrds-request-email.md
docs/FINAL_RESULTS.md
docs/RESULTS.md
# docs/figures/ is deliberately TRACKED. It was ignored while the figures
# carried retracted single-split numbers, but PAPER.md embeds eleven of them,
# so ignoring them rendered every image in the published paper as a broken
# link. They are regenerated by `make paper` and are ~1.4 MB in total.
